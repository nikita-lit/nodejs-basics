<!DOCTYPE html>
<html lang="et">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <div class="card">
            <div class="card-body">
                <h2 class="card-title mb-3"><%= news.title %></h2>

                <% if (news.image) { %>
                    <img src="/uploads/<%= news.image %>" class="img-fluid mb-3" style="max-width:400px;">
                <% } %>

                <p class="card-text">ID: <%= news.id %></p>
                <p class="card-text"><%= news.content %></p>

                <p class="text-muted"><small>Postitatud: <%= news.date %></small></p>

                <a href="/" class="btn btn-secondary mt-3">Tagasi avalehele</a>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
